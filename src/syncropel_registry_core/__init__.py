"""Syncropel Registry Core — shared governance logic.

Pure Python, zero external dependencies. Used by both syncropel-cli and
syncropel-registry to ensure identical governance decisions.
"""

__version__ = "0.2.0"
